Front-end: Send a GET request to the backend
Back-end: API queries the database for recent posts and returns as JSON array

Data flow: 
GET request → API → Database query.
JSON response → Feed rendered on the front-end.